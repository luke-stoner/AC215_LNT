# PIPELINE DEFINITION
# Name: lnt-pipeline
# Inputs:
#    project_id: str
components:
  comp-run-image-step:
    executorLabel: exec-run-image-step
    inputDefinitions:
      parameters:
        image_uri:
          parameterType: STRING
        name:
          parameterType: STRING
  comp-run-image-step-2:
    executorLabel: exec-run-image-step-2
    inputDefinitions:
      parameters:
        image_uri:
          parameterType: STRING
        name:
          parameterType: STRING
  comp-run-image-step-3:
    executorLabel: exec-run-image-step-3
    inputDefinitions:
      parameters:
        image_uri:
          parameterType: STRING
        name:
          parameterType: STRING
defaultPipelineRoot: gs://models-lnt/pipeline
deploymentSpec:
  executors:
    exec-run-image-step:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - run_image_step
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'kfp==2.3.0'\
          \ '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"\
          $0\" \"$@\"\n"
        - sh
        - -ec
        - 'program_path=$(mktemp -d)


          printf "%s" "$0" > "$program_path/ephemeral_component.py"

          _KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         "$program_path/ephemeral_component.py"                         "$@"

          '
        - "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import\
          \ *\n\ndef run_image_step(\n    name: str,\n    image_uri: str\n):\n   \
          \ return {\n       'name': name,\n       'image': image_uri\n    }\n\n"
        image: python:3.7
    exec-run-image-step-2:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - run_image_step
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'kfp==2.3.0'\
          \ '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"\
          $0\" \"$@\"\n"
        - sh
        - -ec
        - 'program_path=$(mktemp -d)


          printf "%s" "$0" > "$program_path/ephemeral_component.py"

          _KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         "$program_path/ephemeral_component.py"                         "$@"

          '
        - "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import\
          \ *\n\ndef run_image_step(\n    name: str,\n    image_uri: str\n):\n   \
          \ return {\n       'name': name,\n       'image': image_uri\n    }\n\n"
        image: python:3.7
    exec-run-image-step-3:
      container:
        args:
        - --executor_input
        - '{{$}}'
        - --function_to_execute
        - run_image_step
        command:
        - sh
        - -c
        - "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip ||\
          \ python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1\
          \ python3 -m pip install --quiet --no-warn-script-location 'kfp==2.3.0'\
          \ '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"\
          $0\" \"$@\"\n"
        - sh
        - -ec
        - 'program_path=$(mktemp -d)


          printf "%s" "$0" > "$program_path/ephemeral_component.py"

          _KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         "$program_path/ephemeral_component.py"                         "$@"

          '
        - "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import\
          \ *\n\ndef run_image_step(\n    name: str,\n    image_uri: str\n):\n   \
          \ return {\n       'name': name,\n       'image': image_uri\n    }\n\n"
        image: python:3.7
pipelineInfo:
  name: lnt-pipeline
root:
  dag:
    tasks:
      run-image-step:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-run-image-step
        inputs:
          parameters:
            image_uri:
              runtimeValue:
                constant: us-east4-docker.pkg.dev/ac215-400221/lnt-repository/lnt-scrape:1.0.1
            name:
              runtimeValue:
                constant: scrape
        taskInfo:
          name: run-image-step
      run-image-step-2:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-run-image-step-2
        dependentTasks:
        - run-image-step
        inputs:
          parameters:
            image_uri:
              runtimeValue:
                constant: us-east4-docker.pkg.dev/ac215-400221/lnt-repository/lnt-label:1.1.7
            name:
              runtimeValue:
                constant: label
        taskInfo:
          name: run-image-step-2
      run-image-step-3:
        cachingOptions:
          enableCache: true
        componentRef:
          name: comp-run-image-step-3
        dependentTasks:
        - run-image-step-2
        inputs:
          parameters:
            image_uri:
              runtimeValue:
                constant: us-east4-docker.pkg.dev/ac215-400221/lnt-repository/lnt-summarize:1.0.1
            name:
              runtimeValue:
                constant: summarize
        taskInfo:
          name: run-image-step-3
  inputDefinitions:
    parameters:
      project_id:
        parameterType: STRING
schemaVersion: 2.1.0
sdkVersion: kfp-2.3.0
